import './globals.css';
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'CodewareAI - Intelligent AI Solutions',
  description: 'Empowering businesses with intelligent AI solutions for enhanced customer engagement and automation. Based in Kingston, Jamaica and Miami, Florida.',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
